services:
  sincronizador-pensiones:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    command: ["sh", "-c", "while true; do echo 'Ejecutando sincronizacion...'; python Sync_Folios.py; echo 'Esperando 60s...'; sleep 60; done"]